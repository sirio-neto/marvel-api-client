import { StyleSheet } from 'react-native';

export default StyleSheet.create({
	overlay: {
		opacity: 0.1,
		backgroundColor: 'black',
		width: '100%',
		height: '100%',
		zIndex: 10,
		top: 0,
		left: 0,
		position: 'absolute'
	}
});